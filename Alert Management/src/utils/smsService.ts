export async function sendSMSAlert(phoneNumber: string, message: string) {
    console.log(`Mock: SMS sent to ${phoneNumber}: ${message}`);
    // Simulating sending an SMS without actually sending it
  }